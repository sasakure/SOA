<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @post.title %>
</p>

<p>
  <b>Body:</b>
  <%= @post.body %>
</p>

<p>
  <b>commnets:</b>
  <% @post.commnets.each do |comment| %>
    <p>
      <%= "#{comment.commnet} by #{comment.name}" %>
   </p>
  <% end %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>

<hr>
<%= render @post.comments.build %>
